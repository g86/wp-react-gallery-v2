# WIP: WP Gallery Plugin
WP independent gallery via PHP API, managed with React based interface via admin panel.

## Plugin deployables
Files and folders: build, classes, php-includes, api.php, include-gallery-plugin.php